# 结构说明

## 资源目录
- 用户上传资源：`./cache_dir/uploads`
- 网络下载资源：`./cache_dir/net_store`
- 预置资源：`./cache_dir/pre_stored`
- 生成文件：`./cache_dir/generate`
- 为了方便管理，提示词统一放在`./提示词.py`

## 智能体功能
- 网络智能体：搜寻URL资源下载到指定目录并返回路径，或直接总结网页内容
- 编程智能体：合并了原代码智能体与命令执行智能体功能，便于直接编写并执行代码

## 优化要求
- 清晰描述每个智能体和MCP工具的使用方法,以便其他智能体感知
- 每个智能体的提示词最好先从prompts.py CV复制，并根据职责补充内容
- 每个智能体的提示词中，应对处理的任务进行描述，尤其要根据测试中出现的问题，补充提示词
## 12.8日更新：
- 添加了command_tool，使用mcp执行类似‘python ****.py’的命令时线程会卡住，其他命令正常，知道如何解决的请留言，可能和启动工具uvicorn有关
- 在preset_tools中添加了command_tool，用于执行命令，但依然无法处理含非utf-8字符的命令，期待后续解决，与单字符读取有关，可以尝试取消单字符读取，但会降低效率
## 12.10日更新：
-command_handler使用sse传输可以避免线程卡死的问题，且原有避免因读取问题而处理命令的代码过于冗余，后续应精简
## 12.20日更新：
-添加了RAGflow的读取，但好像没什么用，后续可以添加上传文件功能，RAGflow需要另外配置
-简化了search的实现，添加了从URL下载文件的功能(虽然命令行也可以实现，但还是单独实现，方便文件管理)
-提示词不要直接写到代码里，不好集中管理，可以写到提示词.py里，然后通过import导入
-注意更新requirements.txt